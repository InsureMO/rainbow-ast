export enum TokenAttributes {
	/** kind of type, one of @interface, class, enum, interface, record or trait */
	TypeKind = 'TypeKind',
	/** name of type */
	TypeName = 'TypeName',
}

export const TA = TokenAttributes;
