{
	"private": true,
	"workspaces": [
		"core",
		"java-base",
		"groovy",
		"cm-editor",
		"cm-groovy"
	],
	"scripts": {
		"core:build": "cd ./core && yarn build",
		"java-base:build": "cd ./java-base && yarn build",
		"groovy:build": "cd ./groovy && yarn build",
		"cm-editor:build": "cd ./cm-editor && yarn build",
		"cm-groovy:build": "cd ./cm-groovy && yarn build",
		"all:build": "yarn core:build && yarn java-base:build && yarn groovy:build"
	},
	"volta": {
		"node": "22.11.0",
		"yarn": "1.22.22"
	}
}
